import java.util.*;
import java.io.*;

public class Driver {
    public static void main(String[] args){
        WordSearch w = new WordSearch();
        System.out.println(w);
	w.loadWords(wordList);
	for (int i = 0; i < wordList.size(); i++){
	    w.addWordRand(wordList.get(i));
	}
	System.out.println(w);
	System.out.println(words.toString()):
	    
 }
}
