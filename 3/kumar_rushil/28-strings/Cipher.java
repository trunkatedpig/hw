public class Cipher{
    public String runCipher(String s, int n){
	String result = "";
	s = s.toLowerCase();
	for (int i = 0; i < s.length(); i ++){
	    if (s.charAt(i) + n < 'z'){
		
	    }
	}
	return result;
    }
}
