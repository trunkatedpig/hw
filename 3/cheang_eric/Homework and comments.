/* 
DRIVER
*/


import java.io.*;
import java.util.*;

public class Driver {
    public static void main(String[] args) {
	Greeter g,g2;
	String result;
	g = new Greeter();
	g.setGreeting("Howdy");
	g2 = new Greeter();
	g2.setGreeting("How are you doing");
	result = g.greet("Tom");
	System.out.println(result);
	result = g2.greet("Fred");
	System.out.println(result);
	g.setGreeting("Howdy");
        g.greet("Tom");
        g.setGreeting("sup");
        g.greet("Fred");
	

	/* 1. Try:
	   g.setGreeting("Howdy");
	   g.greet("Tom");
	   g.setGreeting("sup");
	   g.greet("Fred");
	*/


    }
}


/* 
1.
   Note to Self:
   
   Order goes from top to bottom, bottom taking precedence. 
   Nothing New.
   Still extremely confusing.

*/


//Greeter

import java.io.*;
import java.util.*;

public class Greeter {

    public static  String greeting; 
    /* 2. change public to private,
       test, then change back 
    */


    /* 3. change public to private,
       test, then change back 
    */
    public void setGreeting(String greet) {
	greeting = greet;
    }

    public String greet(String name) {
	String message;
	message = greeting + " " + name;
	return message;
    }
}

/* 
2. 
   Got backhanded by my cmd.
   Error: private access in Greeter g.greet("tom") when compiling driver.java
   
   This must mean that when compiling, it references THESE methods and classes.
   Because they're private, Driver.java can't be compiled because the
   main backbone of it is unable to be converted.
*/


/*
  Get How are you doing for both, even though first time around 
  it should be howdy.
  Maybe static means static variables/memory cells?
  Man I don't know, tiny brain cannot comprehend big boy languages.
  Abandon ship.
*/
