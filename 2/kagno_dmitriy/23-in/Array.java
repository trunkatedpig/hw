import java.util.*;
import java.io.*;

public class Array{
    public void reverse(ArrayList name){
	for (int i = 0; i < name.size() / 2;i++){
	    name.set(i) = name.get(name.size() - 1);
	}
    }
    
    public ArrayList
}